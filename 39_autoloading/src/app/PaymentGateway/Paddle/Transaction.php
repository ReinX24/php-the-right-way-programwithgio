<?php

declare(strict_types=1);

namespace App\PaymentGateway\Paddle;

// use DateTime; // using built in date time
// use Notification\Email;

class Transaction
{
    public function __construct()
    {
        // var_dump(new DateTime());
        // var_dump(new Email());
        // var_dump(explode(",", "hello,world"));
    }
}
